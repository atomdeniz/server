---
- name: Restart amnezia-wg
  # Restarting re-runs awg-quick up, which regenerates wg0.conf from the DB, so
  # DB-level changes (the corrected masquerade hook, AWG 2.0 params) take effect.
  community.docker.docker_container:
    name: "amnezia-wg"
    restart: true
