---
- name: Restart amnezia-wg to apply masquerade hook
  # Restarting re-runs awg-quick up, which regenerates wg0.conf from the (now
  # corrected) DB hook, so the device-agnostic masquerade rule takes effect.
  community.docker.docker_container:
    name: "amnezia-wg"
    restart: true
