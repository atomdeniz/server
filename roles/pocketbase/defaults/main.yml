---
# PocketBase — open-source backend (single Go binary + SQLite) used as the
# waitlist / lead-capture backend for the landing page.
pocketbase_version: "0.39.6"

# Admin (superuser) account. The password MUST be defined in secret.yml (vault):
#   pocketbase_admin_password: "<a-strong-password>"
pocketbase_admin_email: "{{ email_recipient }}"
