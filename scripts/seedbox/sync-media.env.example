# Seedbox sync runtime config. Copy to ~/.config/sync-media.env on the
# seedbox and fill in real values. The sync script sources this file if
# present; ntfy notifications are skipped gracefully if either value is
# missing.
NTFY_URL=https://ntfy.example.com/your-topic
NTFY_TOKEN=tk_replace_me
